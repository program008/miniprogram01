//合并类别的名字
var toString = function (children) {
  var childrens = ""
  for (var i = 0; i < children.length; ++i) {
    var c = children[i]
    childrens += c.name +"  " 
  }
  console.log("合并后的字符串：", childrens)
  return childrens
}
module.exports = {
  string: toString
}